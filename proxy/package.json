{
	"name": "delphi-assistant-proxy",
	"version": "1.0.0",
	"private": true,
	"description": "Navigation-request proxy for the DELPHI assistant. Holds the API key, the cache and the spend ceiling.",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"tail": "wrangler tail"
	},
	"dependencies": {
		"@anthropic-ai/sdk": "^0.68.0"
	},
	"devDependencies": {
		"wrangler": "^4.0.0"
	}
}
